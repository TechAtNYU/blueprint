/**
 * TNYU Controller
 * Kept blank if anything is ever needed to be processed before the main-ctrl
 * runs.
 */
'use strict';

angular
    .module('app.controllers')
    .controller('TNYUCtrl', function($scope, $location, Restangular) {
    });